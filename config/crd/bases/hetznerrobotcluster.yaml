---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: hetznerrobotclusters.infrastructure.cluster.x-k8s.io
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.0
  labels:
    cluster.x-k8s.io/v1beta1: v1alpha1
spec:
  group: infrastructure.cluster.x-k8s.io
  names:
    categories:
    - cluster-api
    kind: HetznerRobotCluster
    listKind: HetznerRobotClusterList
    plural: hetznerrobotclusters
    singular: hetznerrobotcluster
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .metadata.labels.cluster\.x-k8s\.io/cluster-name
      name: Cluster
      type: string
    - jsonPath: .status.ready
      name: Ready
      type: string
    - jsonPath: .spec.controlPlaneEndpoint.host
      name: Endpoint
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: HetznerRobotCluster is the Schema for the hetznerrobotclusters API.
        properties:
          apiVersion:
            type: string
          kind:
            type: string
          metadata:
            type: object
          spec:
            description: HetznerRobotClusterSpec defines the desired state of HetznerRobotCluster.
            properties:
              controlPlaneEndpoint:
                properties:
                  host:
                    type: string
                  port:
                    format: int32
                    type: integer
                required:
                - host
                - port
                type: object
              robotSecretRef:
                description: "RobotSecretRef references the secret containing Hetzner Robot API credentials (keys: robot-user, robot-password)."
                properties:
                  name:
                    type: string
                  namespace:
                    type: string
                type: object
              sshSecretRef:
                description: "SSHSecretRef references the secret with SSH key for rescue access (keys: ssh-privatekey, ssh-fingerprint)."
                properties:
                  name:
                    type: string
                  namespace:
                    type: string
                type: object
              talosFactoryBaseURL:
                description: TalosFactoryBaseURL is the base URL for Talos factory images. Defaults to https://factory.talos.dev
                type: string
            required:
            - robotSecretRef
            - sshSecretRef
            type: object
          status:
            description: HetznerRobotClusterStatus defines the observed state of HetznerRobotCluster.
            properties:
              conditions:
                items:
                  description: Condition defines an observation of a Cluster API resource operational state.
                  properties:
                    lastTransitionTime:
                      format: date-time
                      type: string
                    message:
                      type: string
                    reason:
                      type: string
                    severity:
                      type: string
                    status:
                      type: string
                    type:
                      type: string
                  required:
                  - lastTransitionTime
                  - status
                  - type
                  type: object
                type: array
              ready:
                type: boolean
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
