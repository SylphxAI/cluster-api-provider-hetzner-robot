---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.5
  name: hetznerrobotremediations.infrastructure.cluster.x-k8s.io
spec:
  group: infrastructure.cluster.x-k8s.io
  names:
    categories:
    - cluster-api
    kind: HetznerRobotRemediation
    listKind: HetznerRobotRemediationList
    plural: hetznerrobotremediations
    singular: hetznerrobotremediation
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.retryCount
      name: RetryCount
      type: integer
    - jsonPath: .status.lastRemediated
      name: LastRemediated
      type: date
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          HetznerRobotRemediation is the Schema for the hetznerrobotremediations API.
          Created by MachineHealthCheck when a Machine is detected as unhealthy.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: HetznerRobotRemediationSpec defines the desired state of
              HetznerRobotRemediation.
            properties:
              strategy:
                description: Strategy defines the remediation strategy to use.
                properties:
                  retryLimit:
                    default: 1
                    description: |-
                      RetryLimit is the maximum number of hardware resets before giving up.
                      When exhausted, the remediation enters Deleting phase and CAPI handles Machine replacement.
                    minimum: 1
                    type: integer
                  timeout:
                    default: 300s
                    description: |-
                      Timeout is the duration to wait after a hardware reset for the node to recover
                      before considering the attempt failed.
                    type: string
                  type:
                    default: Reboot
                    description: Type of remediation. Currently only "Reboot" (hardware
                      reset via Robot API).
                    enum:
                    - Reboot
                    type: string
                type: object
            type: object
          status:
            description: HetznerRobotRemediationStatus defines the observed state
              of HetznerRobotRemediation.
            properties:
              lastRemediated:
                description: LastRemediated is the timestamp of the last hardware
                  reset.
                format: date-time
                type: string
              phase:
                description: Phase represents the current phase of remediation.
                type: string
              retryCount:
                description: RetryCount is the number of hardware resets attempted
                  so far.
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
