---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.5
  name: hetznerrobotmachinetemplates.infrastructure.cluster.x-k8s.io
spec:
  group: infrastructure.cluster.x-k8s.io
  names:
    categories:
    - cluster-api
    kind: HetznerRobotMachineTemplate
    listKind: HetznerRobotMachineTemplateList
    plural: hetznerrobotmachinetemplates
    singular: hetznerrobotmachinetemplate
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: HetznerRobotMachineTemplate is the Schema for the hetznerrobotmachinetemplates
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: HetznerRobotMachineTemplateSpec defines the desired state
              of HetznerRobotMachineTemplate.
            properties:
              template:
                description: HetznerRobotMachineTemplateResource describes the data
                  needed to create a HetznerRobotMachine from a template.
                properties:
                  metadata:
                    type: object
                  spec:
                    description: HetznerRobotMachineSpec defines the desired state
                      of HetznerRobotMachine.
                    properties:
                      hostRef:
                        description: |-
                          HostRef is a direct reference to a specific HetznerRobotHost by name.
                          Mutually exclusive with HostSelector. Use this for static per-server assignments.
                        properties:
                          name:
                            default: ""
                            description: |-
                              Name of the referent.
                              This field is effectively required, but due to backwards compatibility is
                              allowed to be empty. Instances of this type with an empty value here are
                              almost certainly wrong.
                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                            type: string
                        type: object
                        x-kubernetes-map-type: atomic
                      hostSelector:
                        description: |-
                          HostSelector selects an Available HetznerRobotHost by label.
                          Mutually exclusive with HostRef. Use this for dynamic pool claiming.
                        properties:
                          matchExpressions:
                            description: matchExpressions is a list of label selector
                              requirements. The requirements are ANDed.
                            items:
                              description: |-
                                A label selector requirement is a selector that contains values, a key, and an operator that
                                relates the key and values.
                              properties:
                                key:
                                  description: key is the label key that the selector
                                    applies to.
                                  type: string
                                operator:
                                  description: |-
                                    operator represents a key's relationship to a set of values.
                                    Valid operators are In, NotIn, Exists and DoesNotExist.
                                  type: string
                                values:
                                  description: |-
                                    values is an array of string values. If the operator is In or NotIn,
                                    the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                    the values array must be empty. This array is replaced during a strategic
                                    merge patch.
                                  items:
                                    type: string
                                  type: array
                                  x-kubernetes-list-type: atomic
                              required:
                              - key
                              - operator
                              type: object
                            type: array
                            x-kubernetes-list-type: atomic
                          matchLabels:
                            additionalProperties:
                              type: string
                            description: |-
                              matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                              map is equivalent to an element of matchExpressions, whose key field is "key", the
                              operator is "In", and the values array contains only "value". The requirements are ANDed.
                            type: object
                        type: object
                        x-kubernetes-map-type: atomic
                      installDisk:
                        default: /dev/nvme0n1
                        description: |-
                          InstallDisk is the disk to install Talos on.
                          Defaults to /dev/nvme0n1
                        type: string
                      providerID:
                        description: |-
                          ProviderID is the unique identifier for this machine.
                          Set automatically by the controller.
                        type: string
                      talosSchematic:
                        description: |-
                          TalosSchematic is the Talos factory schematic ID (with extensions).
                          Example: 3da7f440f279f4814fa73bdf83c84710a8e93c40a4a3cbba4d969f14afb96298
                        type: string
                      talosVersion:
                        description: |-
                          TalosVersion is the Talos version to install.
                          Example: v1.12.4
                        type: string
                    required:
                    - talosSchematic
                    - talosVersion
                    type: object
                required:
                - spec
                type: object
            required:
            - template
            type: object
          status:
            description: HetznerRobotMachineTemplateStatus defines the observed state
              of HetznerRobotMachineTemplate.
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
